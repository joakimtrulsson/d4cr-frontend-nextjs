import '../../styles/components/buttons/primary-button.scss'

const PrimaryButton = ({title}) => {

    return (
        <button className='primary-button'>{title}</button>
    )
}

export default PrimaryButton