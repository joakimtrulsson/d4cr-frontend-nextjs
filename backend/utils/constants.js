export const PORT = 3000;

export const BASE_URL = `http://localhost:${PORT}/`;
export const API_URL = `${BASE_URL}api/graphql`;

export const MEDIA_URL = `${BASE_URL}public/media`;
export const IMAGE_URL = `${BASE_URL}public/images`;
